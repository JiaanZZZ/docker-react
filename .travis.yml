sudo: required
services:
  - docker


before_install:
  - docker build -t jiaanzhu/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jiaanzhu/docker-react npm run test -- --coverage

